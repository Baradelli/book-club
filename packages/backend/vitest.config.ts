import { defineConfig } from 'vitest/config';

// A configuração real vive em vitest.workspace.ts (projetos `unit` e
// `integration`). Este arquivo fica vazio de propósito.
export default defineConfig({});
